# Agents

This module will contain the LangGraph parent graph, shared state, router, typed handoff, read-only recommendation agent, and constrained order-agent graph.

The recommendation agent may use RAG and web search. The order agent may only produce validated commands for deterministic order services.

