# API client

Placeholder package. The project currently does **not** use a generated OpenAPI client. The hand-written, typed API client lives in [`apps/web/src/shared/api/client.ts`](../../apps/web/src/shared/api/client.ts) alongside its request/response types and SSE parsing logic.

If a generated client is introduced later, it should be published here and re-exported by the web app.
